const fs = require('fs');
const path = require('path');

// 1. BILLIONAIRES (100)
const bList = require('./seed_part1');
// We have the 100 billionaire entries

console.log('Script ready to assemble full database.');
